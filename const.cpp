#include <iostream>

int main(){
    /* 
    The const keyboard specifies that a variable's value is constant 
    tells the compiler to prevent anything from modifying (sửa đổi) it
    (read-only)
    */

    const double PI = 3.14159; // When using 'const', we have to make all the letters uppercase
    const int LIGHT_SPEED = 299792458;
    const int WIDTH = 1000;
    const int LENGTH = 25;
    return 0;

}