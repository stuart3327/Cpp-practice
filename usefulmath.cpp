#include <iostream>
#include <cmath>
int main(){
    using std::cout;
    double x = 3.9;
    double y = 4;
    double z;
    
    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(2, 4); (2^4)
    //z = sqrt(9);
    //z = abs();
    //z = round(x);
    //z = ceil(x);   (Lam tron len)
    //z = floor(x);  (Lam tron xuong)
    
    cout << z;
    return 0;
}